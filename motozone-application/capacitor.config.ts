import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'motozone application',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
